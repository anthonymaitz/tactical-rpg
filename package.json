{
  "name": "tactical-rpg",
  "private": true,
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit -p tsconfig.check.json"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vitest": "^1.6.0"
  },
  "pnpm": {
    "patchedDependencies": {
      "bun-serve-express@1.0.5": "patches/bun-serve-express@1.0.5.patch"
    }
  }
}
